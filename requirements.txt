# ApothecaryDaemon - No external dependencies required
# The application uses only Python standard library (including sqlite3)

# Optional: For future enhancements
# requests>=2.31.0  # For API integrations
